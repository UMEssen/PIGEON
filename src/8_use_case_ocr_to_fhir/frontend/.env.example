# API base URL override (only needed for production builds or non-proxy setups)
# In development, the Vite dev server proxies /api to the backend automatically.
# VITE_API_BASE_URL=/api

# Backend URL for the Vite dev-server proxy (default: http://localhost:8003)
# VITE_BACKEND_URL=http://localhost:8003
